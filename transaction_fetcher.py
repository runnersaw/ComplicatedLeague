from transaction import Transaction, DRAFT

class TransactionFetcher():
	def __init__():
		pass

	def fetch():
		return [
			("Drew Brees", Transaction(DRAFT, 2, 2019)),
			("Drew Brees", Transaction(DRAFT, 2, 2019)),
			("Drew Brees", Transaction(DRAFT, 2, 2019)),
			("Drew Brees", Transaction(DRAFT, 2, 2019)),
			("Drew Brees", Transaction(DRAFT, 2, 2019)),
		]